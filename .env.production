# 운영 빌드 전용 환경변수 (커밋됨 — 비밀 아님)
# Cloudflare Pages / Firebase Hosting 빌드 시 Vite가 자동 주입.
# 정적 호스팅엔 API 프록시가 없으므로, 외부 API(시세/뉴스/LLM)는 이 Worker를 경유해야 함.
# Firebase 설정값은 Cloudflare Pages 대시보드 환경변수에서 주입(여기 중복 불필요).
VITE_API_BASE=https://meatgo-api.jays-f8d.workers.dev
