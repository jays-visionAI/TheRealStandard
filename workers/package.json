{
  "name": "meatgo-api",
  "version": "1.0.0",
  "private": true,
  "description": "MeatGo API gateway (Cloudflare Worker) — 외부 시세/뉴스 CORS 프록시 + 비밀키 처리 + 자동화 엔드포인트",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "wrangler": "^3.86.0"
  }
}
